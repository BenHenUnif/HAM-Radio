
// fichier .cpp : contient le code des fonctions (cpp = C++, un langage de programmation plus 'étendu/large' que le C standard)

// implémentation des fonctions : ici on défini le nom de la fonction, le type des arguments et le nom des arguments

int somme_bibi(int a, int b)
{
  return (a + b);
}

int multi_bibi(int a, int b)
{
  int mul = a * b;
  return (mul);
}

void affiche_page(int num_page)   // 'void' veut dire que la fonction ne retourne PAS de valeur après avoir fait le traitement
{
  // se positionner sur la page 'num_page'
  // afficher la page à l'écran
  
  // ici PAS de return() !
}

